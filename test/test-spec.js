var assert = require('assert');

describe('Array', function() {
    
    it('should equal 1', function() {
        assert.equal(1,1, 'it is one');
    });
    
    it('should equal 0', function() {
        assert.equal(1,0, 'it is zero');
    });

});